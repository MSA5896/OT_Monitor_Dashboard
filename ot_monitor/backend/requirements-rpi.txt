# requirements-rpi.txt — Extra packages for Raspberry Pi hardware deployment
# Install ONLY on the Raspberry Pi, not on development machines.
#
# Usage:
#   pip install -r requirements.txt          # install base packages first
#   pip install -r requirements-rpi.txt      # then install RPi-specific ones

# ── I2C bus access ────────────────────────────────────────────────────────────
smbus2>=0.4

# ── Adafruit CircuitPython layer ──────────────────────────────────────────────
# adafruit-blinka wraps RPi GPIO/I2C so CircuitPython sensor libs work on Linux.
adafruit-blinka>=8.0

# ── SCD30 — CO₂, Temperature, Humidity (I2C 0x61) ───────────────────────────
adafruit-circuitpython-scd30>=2.3.0

# ── BME280 — Barometric Pressure (I2C 0x76) ──────────────────────────────────
adafruit-circuitpython-bme280>=2.6.0

# Note: PMS5003 uses pyserial only (already in requirements.txt). No extra package needed.
